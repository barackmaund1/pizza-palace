// business logic
// constructor function
function selectedPizza(pname,psize,pcrust,ptopping,total) {
    this.pname=name;
    this.psize=size;
    this.pcrust=crust;
    this.ptopping=[];
    this.total=total;
}
selectedPizza.prototype.total=function() {
    return this. price + crustPrice + toppingprice 
}